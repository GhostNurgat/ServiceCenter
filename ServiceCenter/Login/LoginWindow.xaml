<Window x:Class="ServiceCenter.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceCenter.Login"
        mc:Ignorable="d"
        Title="Вход в систему" Height="194.076" Width="233.768" ResizeMode="NoResize">
    <StackPanel>
        <Label Content="Введите код:" Style="{StaticResource HeaderLabel_2}" />
        <PasswordBox x:Name="UserCode" Margin="0,20,0,0"
                     Width="65" FontSize="14"
                     KeyDown="UserCode_KeyDown" />
        <Button Content="Войти" Style="{StaticResource MenuButton}"
                HorizontalAlignment="Center" Margin="0,20,0,0"
                Width="85" Click="LoginClick" />
        <Button Content="Отмена" Style="{StaticResource MenuButton}"
                HorizontalAlignment="Center" Width="85"
                Click="CancelClick" />
    </StackPanel>
</Window>
